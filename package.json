{
  "name": "gia-linh-messenger-webhook",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
